import React from 'react';


/*
  To Render Drop down List in dropdown
*/
const CustomerItem = (props) => (
  <label>{props.first + " " + props.last}</label>  
);

export default CustomerItem;